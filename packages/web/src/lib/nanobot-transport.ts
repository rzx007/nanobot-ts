/**
 * Nanobot Transport - 重导出 DefaultChatTransport
 *
 * 后端已经使用 AI SDK 标准格式，无需自定义处理
 * 直接使用 DefaultChatTransport 即可
 */
export { DefaultChatTransport } from 'ai';
export type { HttpChatTransportInitOptions } from 'ai';
